apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: crm-saas
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None   # Headless para acceso directo al pod
