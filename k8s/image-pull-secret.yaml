# Este archivo ya no es necesario: los packages GHCR son publicos.
# Conservado como referencia. No tiene recursos k8s para aplicar.
# Si necesitas volver a privado:
#
# kubectl create secret docker-registry ghcr-secret \
#   --namespace crm-saas \
#   --docker-server=ghcr.io \
#   --docker-username=MAtucR \
#   --docker-password=<PAT>
